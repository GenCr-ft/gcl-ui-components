{
  "name": "@gencraft/gcl-ui-components",
  "version": "0.0.1",
  "description": "Shared UI component library for the Aethel platform (pre-implementation stub).",
  "private": true,
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0"
  }
}
